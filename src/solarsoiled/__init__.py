"""SolarSoiled — customer-facing package layer.

Houses the cross-stage primitives (manifests, AOI, model registry, CLI) that
sit on top of the existing research scripts in ``scripts/``.
"""

__version__ = "0.5.0"
