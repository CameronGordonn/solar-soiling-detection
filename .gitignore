# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
#poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
#pdm.lock
#pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
#pixi.lock
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#   in the .venv directory. It is recommended not to include this directory in version control.
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Abstra
# Abstra is an AI-powered process automation framework.
# Ignore directories containing user credentials, local state, and settings.
# Learn more at https://abstra.io/docs
.abstra/

# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore 
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  and can be added to the global gitignore or merged into this file. However, if you prefer, 
#  you could uncomment the following to ignore the entire vscode folder
# .vscode/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# PlanetScope + geospatial raster data
*.tif
*.tiff
*.jp2
*.vrt
*.img
*.h5
*.hdf5
*.nc
*.zip
*.tar
*.tar.gz

# YOLO outputs
runs/

# ML model weights
*.pt
*.pth
*.onnx
*.ckpt

# Local data directories (root-anchored: a bare `data/` also matched the
# tracked source under scripts/data/ and silently dropped it from clones).
/data/
/datasets/

# Intermediate geospatial tiles
tiles/
masks/
temp/

# Logs
*.log

# Cache directories
.cache/

# Planet Labs API keys or config
planet.json
planet_config.json
*.planet

# AWS / GCP / Azure credentials
.aws/
.gcp/
azure_credentials.json

# Generic secrets
*.key
*.pem
*.crt

# Per-partner API key registry (use keys.example.yaml as template)
keys.yaml
secrets/
config.yaml
config.yml

# PlanetScope metadata
*.xml
metadata/

# Roboflow workflow directories (large working data)
roboflow_upload/
roboflow_download/

# Local working artifacts (kept out of the repo root; never committed)
backups/
naip.zip
*.zip

# Temporary process files
*.pid
tiling.pid
monitor_tiling.sh

# GDAL / rasterio caches
.gdal/
.rasterio/

*.ipynb_autosave

mlruns/
wandb/
tensorboard/

logs/
*.log
outputs/
archives/

# Agent / tooling internal state — these are local session artifacts, not
# project content. Never commit them.
.claude/*
# ...except the shared lane runbooks: docs/ONBOARDING.md points teammates here,
# so they must ship with a clone (they are project content, not session state).
!.claude/rules/
.codex
.codex/

# Editor / OS junk
.DS_Store
Thumbs.db
*~
*.swp
*.swo

# Local scratch / notes (personal, not for the repo)
scratch/
.scratch/
NOTES.local.md
SESSION_STARTUP.md

# Personal / correspondence docs. Anything here stays on the author's machine.
# Use it for email drafts, funding correspondence, negotiation notes, contacts --
# things that are working material rather than project content a teammate needs
# from a fresh clone. The README is tracked so the convention survives a clone
# even though nothing else in the directory does.
# NOTE: `docs/local/*` not `docs/local/`. Git cannot re-include a file inside an
# excluded DIRECTORY, so the directory form would swallow the README too.
docs/local/*
!docs/local/README.md

# Email drafts anywhere under docs/, by naming convention, so a draft written in
# the wrong directory is still not committed by accident.
docs/**/*_DRAFT.txt
docs/**/*_email_*.txt
!docs/outreach/cpra_city_email_READY.txt

# WSL Zone.Identifier sidecar files (created when downloading via Windows Explorer)
*:Zone.Identifier

# Editor workspace settings (machine-local interpreter paths)
.vscode/

# ── Excluded from the public snapshot ────────────────────────────────────────
# The outreach lane ships neither its data nor the guard that tests it. The CSV
# carries identifiable property data; test_qr_compat.py asserts that CSV is
# checked in, which is a correct guard in the private repo and an automatic
# failure in a distribution that deliberately omits it.
configs/outreach/published_qr_ids.csv                 # 97 street addresses + APNs
outputs/aoi/santa-cruz-w2-21cm/array_install_era.csv  # 3,362 parcel numbers
docs/outreach/                                        # vendor call sheet + CPRA templates
tests/test_qr_compat.py                               # guards the CSV above
